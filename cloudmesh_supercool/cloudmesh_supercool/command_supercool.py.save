from cloudmesh_base.Shell import Shell


class command_supercool(object):

    @classmethod
    def status(file):
	output = location = "~/" + file
	print location
        if [-s location ]:
		return True
	else:
		return False

	
